// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 CLSOFTLAB (씨엘소프트랩), Dr. Lee Il-guk (이일국)
//
// AI endpoint configuration.
//
// DEMO / GitHub Pages: leave this EMPTY. An empty endpoint means the app uses
// the built-in deterministic Korean MockProvider (no network, no API key).
//
// To enable REAL Claude: run the backend proxy in ../server/ (which holds the
// ANTHROPIC_API_KEY server-side) and set this to its public URL, e.g.
//   export const AI_ENDPOINT = "https://your-proxy.example.com/api/ai";
//
// NEVER put an API key in this file or anywhere in the browser/repo.
export const AI_ENDPOINT = "";
